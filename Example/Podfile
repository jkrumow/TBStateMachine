source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '8.0'
target 'Tests' do
  pod 'TBStateMachine', :path => '../'
  pod 'TBStateMachine/Builder', :path => '../'
  pod 'TBStateMachine/DebugSupport', :path => '../'

  pod 'Specta'
  pod 'Expecta'
end
